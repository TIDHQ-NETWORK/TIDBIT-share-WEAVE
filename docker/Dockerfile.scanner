FROM alpine:3.22

RUN apk add --no-cache ca-certificates file
RUN adduser -D -u 10001 scanner

WORKDIR /workspace

USER scanner

HEALTHCHECK --interval=30s --timeout=5s --start-period=5s --retries=3 \
    CMD ["sh", "-c", "test -d /workspace && command -v file >/dev/null"]

CMD ["sh"]
